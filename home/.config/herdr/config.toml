onboarding = false
[keys]
prefix = "ctrl+b"
focus_pane_left  = "prefix+h"
focus_pane_down  = "prefix+j"
focus_pane_up    = "prefix+k"
focus_pane_right = "prefix+l"
split_horizontal = "prefix+double_quote"
split_vertical    = "prefix+percent"
new_tab   = "prefix+c"
close_tab = "prefix+ampersand"
workspace_picker = "prefix+w"
previous_workspace = "prefix+["
next_workspace     = "prefix+]"
goto             = "prefix+g"
copy_mode  = "prefix+y"  # herdr's copy-mode entry key; copy-mode's own internal keys (v/space select, y/Enter copy, q/Esc cancel) aren't configurable

[[keys.command]]
key = "prefix+shift+y"
type = "shell"
command = "$HOME/.dotfiles/home/.local/bin/clean-prose-clipboard"
description = "clean wrapped prose in clipboard"

[theme]
name = "catppuccin"
auto_switch = false
